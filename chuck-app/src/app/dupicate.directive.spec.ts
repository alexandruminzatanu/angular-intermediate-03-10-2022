import { DupicateDirective } from './dupicate.directive';

describe('DupicateDirective', () => {
  it('should create an instance', () => {
    const directive = new DupicateDirective();
    expect(directive).toBeTruthy();
  });
});
